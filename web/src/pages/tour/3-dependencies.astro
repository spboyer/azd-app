---
/**
 * Tour Step 3: Install dependencies
 */
import TourLayout from '../../components/tour/TourLayout.astro';
import CompletionCheckbox from '../../components/tour/CompletionCheckbox.astro';
import TourNavigation from '../../components/tour/TourNavigation.astro';
import LearnMoreSection from '../../components/tour/LearnMoreSection.astro';
import TryItYourself from '../../components/tour/TryItYourself.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import { tourSteps, getStepByNumber, getPreviousStep, getNextStep } from '../../data/tour-steps';

const step = getStepByNumber(3)!;
const prevStep = getPreviousStep(3);
const nextStep = getNextStep(3);
---

<TourLayout 
  title={step.title}
  description={step.description}
  currentStep={step.number}
>
  <article class="tour-step-page">
    <!-- Step Header -->
    <header class="tour-step-header">
      <nav aria-label="Breadcrumb" class="tour-breadcrumb">
        <a href={`${import.meta.env.BASE_URL}tour/`}>Guided Tour</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Step {step.number}</span>
      </nav>
      
      <div class="tour-step-meta">
        <span class="tour-step-icon" aria-hidden="true">{step.icon}</span>
        <span class="tour-step-number">Step {step.number}</span>
      </div>
      
      <h1 class="tour-step-title">{step.title}</h1>
      
      <div class="tour-step-info">
        <span class="tour-step-time">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          ~{step.estimatedTime} minutes
        </span>
      </div>

      <CompletionCheckbox stepNumber={step.number} />
    </header>

    <!-- Step Content -->
    <div class="tour-step-content">
      <p class="tour-step-intro">
        The <code>azd app deps</code> command installs project dependencies and creates virtual environments
        for Python projects. For the demo project, this will install all required packages.
      </p>

      <h2>Install Project Dependencies</h2>
      <p>From the demo project folder, install dependencies:</p>

      <CodeBlock 
        code={`# Make sure you're in the demo project
cd azd-app-demo

# Install dependencies
azd app deps`}
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <p>This command will:</p>
      <ul>
        <li>Detect your project type (.NET Aspire, Docker Compose, etc.)</li>
        <li>Install required NuGet packages for .NET projects</li>
        <li>Install npm/pnpm packages for Node.js projects</li>
        <li>Pull required Docker images</li>
        <li>Set up any project-specific dependencies</li>
      </ul>

      <h2>Python Virtual Environments</h2>
      <p>For Python projects, <code>azd app deps</code> automatically creates a virtual environment if one doesn't exist, then installs packages into it.</p>

      <h2>Expected Output</h2>
      <CodeBlock 
        code={`Installing dependencies...

[dotnet] Restoring packages for MyApp.csproj...
[dotnet] ✓ Restored 42 packages
[npm] Installing packages in dashboard/...
[npm] ✓ Installed 128 packages
[docker] Pulling required images...
[docker] ✓ Pulled postgres:16
[docker] ✓ Pulled redis:7-alpine

All dependencies installed successfully!`}
        language="bash"
        showLanguage={false}
        showCopy={false}
      />

      <TryItYourself 
        prompt="Run `azd app deps` in the demo project directory and watch the installation progress."
        expectedOutcome="You should see packages being restored/installed for each component of your project, ending with a success message."
      />

      <LearnMoreSection id="step3-deps-details" title="Understanding Dependencies">
        <h3>Project Detection</h3>
        <p>
          azd-app automatically detects your project type by looking for configuration files
          like <code>*.csproj</code>, <code>package.json</code>, <code>docker-compose.yml</code>,
          or <code>azure.yaml</code>.
        </p>

        <h3>Dependency Caching</h3>
        <p>
          Dependencies are cached locally to speed up subsequent installations. Use
          <code>azd app deps --clean</code> to remove existing packages first, or
          <code>azd app deps --no-cache</code> to bypass the cache.
        </p>

        <h3>Troubleshooting</h3>
        <ul>
          <li>If installation fails, check your internet connection</li>
          <li>Ensure you have sufficient disk space for Docker images</li>
          <li>Run with <code>--verbose</code> flag for detailed output</li>
        </ul>
      </LearnMoreSection>
    </div>

    <!-- Navigation -->
    <TourNavigation 
      currentStep={step.number}
      totalSteps={tourSteps.length}
      previousStep={prevStep}
      nextStep={nextStep}
    />
  </article>
</TourLayout>

<style>
  .tour-step-page {
    max-width: 100%;
  }

  .tour-step-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border-default);
  }

  .tour-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-bottom: 1rem;
  }

  .tour-breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .tour-breadcrumb a:hover {
    color: var(--color-interactive-default);
    text-decoration: underline;
  }

  .tour-step-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .tour-step-icon {
    font-size: 1.5rem;
  }

  .tour-step-number {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-azure-600);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  [data-theme="dark"] .tour-step-number {
    color: var(--color-azure-400);
  }

  .tour-step-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
  }

  .tour-step-info {
    margin-bottom: 1.5rem;
  }

  .tour-step-time {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .tour-step-content {
    line-height: 1.7;
  }

  .tour-step-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 2rem 0 1rem;
  }

  .tour-step-content h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 1.5rem 0 0.75rem;
  }

  .tour-step-content p {
    color: var(--color-text-secondary);
    margin: 0 0 1rem;
  }

  .tour-step-content ul,
  .tour-step-content ol {
    color: var(--color-text-secondary);
    padding-left: 1.5rem;
    margin: 0 0 1rem;
  }

  .tour-step-content li {
    margin: 0.5rem 0;
  }

  .tour-step-intro {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  @media (max-width: 640px) {
    .tour-step-title {
      font-size: 1.5rem;
    }
  }
</style>
