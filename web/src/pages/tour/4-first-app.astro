---
/**
 * Tour Step 4: Run your first app
 */
import TourLayout from '../../components/tour/TourLayout.astro';
import CompletionCheckbox from '../../components/tour/CompletionCheckbox.astro';
import TourNavigation from '../../components/tour/TourNavigation.astro';
import LearnMoreSection from '../../components/tour/LearnMoreSection.astro';
import TryItYourself from '../../components/tour/TryItYourself.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import { tourSteps, getStepByNumber, getPreviousStep, getNextStep } from '../../data/tour-steps';

const step = getStepByNumber(4)!;
const prevStep = getPreviousStep(4);
const nextStep = getNextStep(4);
---

<TourLayout 
  title={step.title}
  description={step.description}
  currentStep={step.number}
>
  <article class="tour-step-page">
    <!-- Step Header -->
    <header class="tour-step-header">
      <nav aria-label="Breadcrumb" class="tour-breadcrumb">
        <a href={`${import.meta.env.BASE_URL}tour/`}>Guided Tour</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Step {step.number}</span>
      </nav>
      
      <div class="tour-step-meta">
        <span class="tour-step-icon" aria-hidden="true">{step.icon}</span>
        <span class="tour-step-number">Step {step.number}</span>
      </div>
      
      <h1 class="tour-step-title">{step.title}</h1>
      
      <div class="tour-step-info">
        <span class="tour-step-time">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          ~{step.estimatedTime} minutes
        </span>
      </div>

      <CompletionCheckbox stepNumber={step.number} />
    </header>

    <!-- Step Content -->
    <div class="tour-step-content">
      <p class="tour-step-intro">
        Now let's run the demo application! If you followed Step 1, you already have
        the demo project cloned. Let's start all services with a single command.
      </p>

      <h2>Start All Services</h2>
      <p>From the demo project folder, run:</p>

      <CodeBlock 
        code={`# Make sure you're in the demo project
cd azd-app-demo

# Run the application
azd app run`}
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <p>This command will:</p>
      <ul>
        <li>Build all services in your project</li>
        <li>Start containers for databases and other dependencies</li>
        <li>Launch your application services</li>
        <li>Open the azd-app dashboard in your browser</li>
      </ul>

      <h2>Expected Output</h2>
      <CodeBlock 
        code={`Starting azd-app...

Building services...
✓ api built successfully
✓ web built successfully

Starting dependencies...
✓ postgres started on port 5432
✓ redis started on port 6379

Starting services...
✓ api started on http://localhost:5000
✓ web started on http://localhost:3000

Dashboard: http://localhost:5050

Press Ctrl+C to stop all services.`}
        language="bash"
        showLanguage={false}
        showCopy={false}
      />

      <h2>Access Your App</h2>
      <p>Once running, you can access the services at the URLs shown in the terminal output. The dashboard URL is displayed as well (typically at port 4280).</p>

      <TryItYourself 
        prompt="Run `azd app run` and wait for all services to start. Then open the dashboard URL shown in the terminal."
        expectedOutcome="You should see the azd app dashboard with a list of running services and their health status."
      />

      <LearnMoreSection id="step4-run-options" title="Advanced Run Options">
        <h3>Running Specific Services</h3>
        <p>You can run only specific services:</p>
        <CodeBlock 
          code="azd app run --service api,web"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />

        <h3>Open Dashboard in Browser</h3>
        <p>Automatically open the dashboard in your default browser:</p>
        <CodeBlock 
          code="azd app run --web"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />

        <h3>Verbose Output</h3>
        <p>Enable detailed logging to debug issues:</p>
        <CodeBlock 
          code="azd app run --verbose"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />

        <h3>Dry Run</h3>
        <p>Preview what would be executed without starting services:</p>
        <CodeBlock 
          code="azd app run --dry-run"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />
      </LearnMoreSection>
    </div>

    <!-- Navigation -->
    <TourNavigation 
      currentStep={step.number}
      totalSteps={tourSteps.length}
      previousStep={prevStep}
      nextStep={nextStep}
    />
  </article>
</TourLayout>

<style>
  .tour-step-page {
    max-width: 100%;
  }

  .tour-step-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border-default);
  }

  .tour-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-bottom: 1rem;
  }

  .tour-breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .tour-breadcrumb a:hover {
    color: var(--color-interactive-default);
    text-decoration: underline;
  }

  .tour-step-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .tour-step-icon {
    font-size: 1.5rem;
  }

  .tour-step-number {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-azure-600);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  [data-theme="dark"] .tour-step-number {
    color: var(--color-azure-400);
  }

  .tour-step-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
  }

  .tour-step-info {
    margin-bottom: 1.5rem;
  }

  .tour-step-time {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .tour-step-content {
    line-height: 1.7;
  }

  .tour-step-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 2rem 0 1rem;
  }

  .tour-step-content h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 1.5rem 0 0.75rem;
  }

  .tour-step-content p {
    color: var(--color-text-secondary);
    margin: 0 0 1rem;
  }

  .tour-step-content ul,
  .tour-step-content ol {
    color: var(--color-text-secondary);
    padding-left: 1.5rem;
    margin: 0 0 1rem;
  }

  .tour-step-content li {
    margin: 0.5rem 0;
  }

  .tour-step-content kbd {
    display: inline-block;
    padding: 0.125rem 0.5rem;
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border-default);
    border-radius: 0.25rem;
    font-family: var(--font-family-mono);
    font-size: 0.8125rem;
    box-shadow: 0 1px 0 var(--color-border-strong);
  }

  .tour-step-intro {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  @media (max-width: 640px) {
    .tour-step-title {
      font-size: 1.5rem;
    }
  }
</style>
