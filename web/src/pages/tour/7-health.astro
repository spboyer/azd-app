---
/**
 * Tour Step 7: Monitor service health
 */
import TourLayout from '../../components/tour/TourLayout.astro';
import CompletionCheckbox from '../../components/tour/CompletionCheckbox.astro';
import TourNavigation from '../../components/tour/TourNavigation.astro';
import LearnMoreSection from '../../components/tour/LearnMoreSection.astro';
import TryItYourself from '../../components/tour/TryItYourself.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import { tourSteps, getStepByNumber, getPreviousStep, getNextStep } from '../../data/tour-steps';

const step = getStepByNumber(7)!;
const prevStep = getPreviousStep(7);
const nextStep = getNextStep(7);
---

<TourLayout 
  title={step.title}
  description={step.description}
  currentStep={step.number}
>
  <article class="tour-step-page">
    <!-- Step Header -->
    <header class="tour-step-header">
      <nav aria-label="Breadcrumb" class="tour-breadcrumb">
        <a href={`${import.meta.env.BASE_URL}tour/`}>Guided Tour</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Step {step.number}</span>
      </nav>
      
      <div class="tour-step-meta">
        <span class="tour-step-icon" aria-hidden="true">{step.icon}</span>
        <span class="tour-step-number">Step {step.number}</span>
      </div>
      
      <h1 class="tour-step-title">{step.title}</h1>
      
      <div class="tour-step-info">
        <span class="tour-step-time">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          ~{step.estimatedTime} minutes
        </span>
      </div>

      <CompletionCheckbox stepNumber={step.number} />
    </header>

    <!-- Step Content -->
    <div class="tour-step-content">
      <p class="tour-step-intro">
        The <code>azd app health</code> command provides detailed health status for all
        your services, helping you quickly identify and troubleshoot issues.
      </p>

      <h2>Check Health Status</h2>
      <p>Get a health overview of all services:</p>

      <CodeBlock 
        code="azd app health"
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <h2>Expected Output</h2>
      <CodeBlock 
        code={`Service Health Status

┌─────────────┬──────────┬─────────────┬───────────────────┐
│ Service     │ Status   │ Uptime      │ Last Check        │
├─────────────┼──────────┼─────────────┼───────────────────┤
│ api         │ ✓ Healthy│ 45m 23s     │ 2s ago            │
│ web         │ ✓ Healthy│ 45m 22s     │ 2s ago            │
│ postgres    │ ✓ Healthy│ 45m 30s     │ 5s ago            │
│ redis       │ ✓ Healthy│ 45m 29s     │ 3s ago            │
└─────────────┴──────────┴─────────────┴───────────────────┘

All services healthy!`}
        language="bash"
        showLanguage={false}
        showCopy={false}
      />

      <h2>Detailed Health Information</h2>
      <p>Get more details about a specific service:</p>

      <CodeBlock 
        code="azd app health --service api --verbose"
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <h2>Health Check Types</h2>
      <p>azd app supports various health check types based on your service type:</p>
      <ul>
        <li><strong>HTTP</strong> - Checks HTTP endpoints for 2xx responses (default for services with ports)</li>
        <li><strong>TCP</strong> - Verifies TCP port connectivity (for databases, gRPC services)</li>
        <li><strong>Process</strong> - Checks if the process is running (default for services without ports)</li>
        <li><strong>Output</strong> - Matches a regex pattern in stdout (for watch/build services)</li>
      </ul>

      <h2>Understanding Status vs Health</h2>
      <p>azd app tracks two independent concepts:</p>
      <ul>
        <li><strong>Lifecycle State</strong> - Is the process running? (starting, running, stopped, etc.)</li>
        <li><strong>Health Status</strong> - Is the service responding correctly? (healthy, unhealthy, degraded)</li>
      </ul>
      <p>A running service can be unhealthy (process up, but health checks failing), and a stopped service has no health status. The dashboard shows both clearly.</p>

      <h2>Streaming Mode</h2>
      <p>Continuously monitor health status in real-time:</p>

      <CodeBlock 
        code="azd app health --stream"
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <h2>Troubleshooting Unhealthy Services</h2>
      <p>When a service is unhealthy:</p>
      <ol>
        <li>Check the service logs: <code>azd app logs --service &lt;name&gt;</code></li>
        <li>View detailed health info: <code>azd app health --service &lt;name&gt; --verbose</code></li>
        <li>Stop services with Ctrl+C and restart with <code>azd app run</code></li>
      </ol>

      <TryItYourself 
        prompt="Run `azd app health` to see the health status of all your services. Try `--stream` mode to see real-time updates."
        expectedOutcome="You should see a table showing each service's health status, uptime, and when it was last checked."
      />

      <LearnMoreSection id="step7-health-details" title="Advanced Health Monitoring">
        <h3>Custom Health Endpoints</h3>
        <p>Configure custom health check endpoints in your azure.yaml:</p>
        <CodeBlock 
          code={`services:
  api:
    health:
      path: /healthz
      interval: 30s
      timeout: 5s
      retries: 3`}
          language="yaml"
          showLanguage={true}
          showCopy={true}
        />

        <h3>JSON Output</h3>
        <p>For integration with monitoring tools:</p>
        <CodeBlock 
          code="azd app health --output json"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />

        <h3>Exit Codes</h3>
        <ul>
          <li><code>0</code> - All services healthy</li>
          <li><code>1</code> - One or more services unhealthy</li>
          <li><code>2</code> - Error checking health</li>
        </ul>
      </LearnMoreSection>
    </div>

    <!-- Navigation -->
    <TourNavigation 
      currentStep={step.number}
      totalSteps={tourSteps.length}
      previousStep={prevStep}
      nextStep={nextStep}
    />
  </article>
</TourLayout>

<style>
  .tour-step-page {
    max-width: 100%;
  }

  .tour-step-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border-default);
  }

  .tour-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-bottom: 1rem;
  }

  .tour-breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .tour-breadcrumb a:hover {
    color: var(--color-interactive-default);
    text-decoration: underline;
  }

  .tour-step-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .tour-step-icon {
    font-size: 1.5rem;
  }

  .tour-step-number {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-azure-600);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  [data-theme="dark"] .tour-step-number {
    color: var(--color-azure-400);
  }

  .tour-step-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
  }

  .tour-step-info {
    margin-bottom: 1.5rem;
  }

  .tour-step-time {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .tour-step-content {
    line-height: 1.7;
  }

  .tour-step-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 2rem 0 1rem;
  }

  .tour-step-content h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 1.5rem 0 0.75rem;
  }

  .tour-step-content p {
    color: var(--color-text-secondary);
    margin: 0 0 1rem;
  }

  .tour-step-content ul,
  .tour-step-content ol {
    color: var(--color-text-secondary);
    padding-left: 1.5rem;
    margin: 0 0 1rem;
  }

  .tour-step-content li {
    margin: 0.5rem 0;
  }

  .tour-step-intro {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  @media (max-width: 640px) {
    .tour-step-title {
      font-size: 1.5rem;
    }
  }
</style>
