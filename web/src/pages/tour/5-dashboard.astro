---
/**
 * Tour Step 5: Explore the dashboard
 */
import TourLayout from '../../components/tour/TourLayout.astro';
import CompletionCheckbox from '../../components/tour/CompletionCheckbox.astro';
import TourNavigation from '../../components/tour/TourNavigation.astro';
import LearnMoreSection from '../../components/tour/LearnMoreSection.astro';
import TryItYourself from '../../components/tour/TryItYourself.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import { tourSteps, getStepByNumber, getPreviousStep, getNextStep } from '../../data/tour-steps';

const step = getStepByNumber(5)!;
const prevStep = getPreviousStep(5);
const nextStep = getNextStep(5);
---

<TourLayout 
  title={step.title}
  description={step.description}
  currentStep={step.number}
>
  <article class="tour-step-page">
    <!-- Step Header -->
    <header class="tour-step-header">
      <nav aria-label="Breadcrumb" class="tour-breadcrumb">
        <a href={`${import.meta.env.BASE_URL}tour/`}>Guided Tour</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Step {step.number}</span>
      </nav>
      
      <div class="tour-step-meta">
        <span class="tour-step-icon" aria-hidden="true">{step.icon}</span>
        <span class="tour-step-number">Step {step.number}</span>
      </div>
      
      <h1 class="tour-step-title">{step.title}</h1>
      
      <div class="tour-step-info">
        <span class="tour-step-time">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          ~{step.estimatedTime} minutes
        </span>
      </div>

      <CompletionCheckbox stepNumber={step.number} />
    </header>

    <!-- Step Content -->
    <div class="tour-step-content">
      <p class="tour-step-intro">
        The azd app dashboard provides a real-time view of all your running services,
        their health status, logs, and more. Let's explore its features.
      </p>

      <h2>Accessing the Dashboard</h2>
      <p>The dashboard starts automatically when you run <code>azd app run</code>. To automatically open it in your browser, use:</p>

      <CodeBlock 
        code="azd app run --web"
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <p>Or access it directly at the URL shown in the terminal (typically <a href="http://localhost:4280" target="_blank" rel="noopener">http://localhost:4280</a>).</p>

      <h2>Dashboard Features</h2>

      <h3>Services Overview</h3>
      <p>The main view shows all your services with:</p>
      <ul>
        <li><strong>Status indicator</strong> - Green for healthy, red for issues</li>
        <li><strong>Port mappings</strong> - Which ports each service is running on</li>
        <li><strong>Quick actions</strong> - Restart, stop, or open logs for each service</li>
      </ul>

      <h3>Real-time Updates</h3>
      <p>The dashboard updates in real-time:</p>
      <ul>
        <li>Service status changes reflect immediately</li>
        <li>Logs stream live as they're generated</li>
        <li>Resource usage updates every few seconds</li>
      </ul>

      <h3>Theme Toggle</h3>
      <p>Switch between light and dark mode using the theme toggle in the header.</p>

      <h2>Dashboard Sections</h2>
      <ul>
        <li><strong>Services</strong> - Main view of all running services</li>
        <li><strong>Logs</strong> - Combined or per-service log viewer</li>
        <li><strong>Health</strong> - Detailed health check information</li>
        <li><strong>Settings</strong> - Dashboard configuration options</li>
      </ul>

      <TryItYourself 
        prompt="With your app running, open the dashboard and click on different services to see their details."
        expectedOutcome="You should be able to see service status, view logs, and interact with the dashboard controls."
      />

      <LearnMoreSection id="step5-dashboard-customization" title="Dashboard Customization">
        <h3>Opening in Browser</h3>
        <p>Use the <code>--web</code> or <code>-w</code> flag to automatically open the dashboard:</p>
        <CodeBlock 
          code="azd app run -w"
          language="bash"
          showLanguage={true}
          showCopy={true}
        />

        <h3>Dashboard Features</h3>
        <ul>
          <li>Real-time service status and health</li>
          <li>Live log streaming from all services</li>
          <li>Service URL links for quick access</li>
          <li>Light and dark theme support</li>
        </ul>
      </LearnMoreSection>
    </div>

    <!-- Navigation -->
    <TourNavigation 
      currentStep={step.number}
      totalSteps={tourSteps.length}
      previousStep={prevStep}
      nextStep={nextStep}
    />
  </article>
</TourLayout>

<style>
  .tour-step-page {
    max-width: 100%;
  }

  .tour-step-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border-default);
  }

  .tour-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-bottom: 1rem;
  }

  .tour-breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .tour-breadcrumb a:hover {
    color: var(--color-interactive-default);
    text-decoration: underline;
  }

  .tour-step-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .tour-step-icon {
    font-size: 1.5rem;
  }

  .tour-step-number {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-azure-600);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  [data-theme="dark"] .tour-step-number {
    color: var(--color-azure-400);
  }

  .tour-step-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
  }

  .tour-step-info {
    margin-bottom: 1.5rem;
  }

  .tour-step-time {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .tour-step-content {
    line-height: 1.7;
  }

  .tour-step-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 2rem 0 1rem;
  }

  .tour-step-content h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 1.5rem 0 0.75rem;
  }

  .tour-step-content p {
    color: var(--color-text-secondary);
    margin: 0 0 1rem;
  }

  .tour-step-content ul,
  .tour-step-content ol {
    color: var(--color-text-secondary);
    padding-left: 1.5rem;
    margin: 0 0 1rem;
  }

  .tour-step-content li {
    margin: 0.5rem 0;
  }

  .tour-step-content kbd {
    display: inline-block;
    padding: 0.125rem 0.5rem;
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border-default);
    border-radius: 0.25rem;
    font-family: var(--font-family-mono);
    font-size: 0.8125rem;
    box-shadow: 0 1px 0 var(--color-border-strong);
  }

  .tour-step-intro {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  @media (max-width: 640px) {
    .tour-step-title {
      font-size: 1.5rem;
    }
  }
</style>
