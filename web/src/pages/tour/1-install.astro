---
/**
 * Tour Step 1: Install azd + extension
 */
import TourLayout from '../../components/tour/TourLayout.astro';
import CompletionCheckbox from '../../components/tour/CompletionCheckbox.astro';
import TourNavigation from '../../components/tour/TourNavigation.astro';
import LearnMoreSection from '../../components/tour/LearnMoreSection.astro';
import TryItYourself from '../../components/tour/TryItYourself.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import PlatformInstallTabs from '../../components/PlatformInstallTabs.astro';
import { tourSteps, getStepByNumber, getPreviousStep, getNextStep } from '../../data/tour-steps';

const step = getStepByNumber(1)!;
const prevStep = getPreviousStep(1);
const nextStep = getNextStep(1);
---

<TourLayout 
  title={step.title}
  description={step.description}
  currentStep={step.number}
>
  <article class="tour-step-page">
    <!-- Step Header -->
    <header class="tour-step-header">
      <nav aria-label="Breadcrumb" class="tour-breadcrumb">
        <a href={`${import.meta.env.BASE_URL}tour/`}>Guided Tour</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Step {step.number}</span>
      </nav>
      
      <div class="tour-step-meta">
        <span class="tour-step-icon" aria-hidden="true">{step.icon}</span>
        <span class="tour-step-number">Step {step.number}</span>
      </div>
      
      <h1 class="tour-step-title">{step.title}</h1>
      
      <div class="tour-step-info">
        <span class="tour-step-time">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          ~{step.estimatedTime} minutes
        </span>
      </div>

      <CompletionCheckbox stepNumber={step.number} />
    </header>

    <!-- Step Content -->
    <div class="tour-step-content">
      <p class="tour-step-intro">
        Let's start by installing the Azure Developer CLI and the azd app extension.
        This is the foundation for everything else in the tour.
      </p>

      <h2>Prerequisites</h2>
      <ul>
        <li>Windows 10/11, macOS 10.15+, or Ubuntu 20.04+</li>
        <li>Administrator/sudo access for installation</li>
      </ul>

      <h2>Install Azure Developer CLI</h2>
      <p>Choose the installation method for your platform:</p>

      <PlatformInstallTabs id="tour-install" />

      <h2>Install the azd app Extension</h2>
      <p>Once azd is installed, enable extensions and install azd app:</p>

      <CodeBlock 
        code={`# Enable extensions
azd config set alpha.extensions.enabled on

# Add the azd app extension source
azd extension source add -n app -t url -l https://raw.githubusercontent.com/jongio/azd-app/main/registry.json

# Install the extension
azd extension install app`}
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <h2>Clone the Demo Project</h2>
      <p>For this tour, we'll use the demo project which has everything set up for you:</p>

      <CodeBlock 
        code={`# Initialize the demo project
azd init -t jongio/azd-app-demo

# Navigate to the project
cd azd-app-demo`}
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <h2>Verify Installation</h2>
      <CodeBlock 
        code={`# Check azd version
azd version

# Verify the extension is installed
azd app --help`}
        language="bash"
        showLanguage={true}
        showCopy={true}
      />

      <TryItYourself 
        prompt="Run the commands above to install azd app and clone the demo project."
        expectedOutcome="You should have azd installed, the azd app extension working, and be inside the azd-app-demo folder."
      />

      <LearnMoreSection id="step1-alt-install" title="Alternative Installation Methods">
        <h3>Using the MSI Installer (Windows)</h3>
        <p>
          If winget is not available, you can download the MSI installer directly from the 
          <a href="https://github.com/Azure/azure-dev/releases" target="_blank" rel="noopener">Azure Developer CLI releases page</a>.
        </p>
        
        <h3>Using a Script (All Platforms)</h3>
        <p>
          The installation script can be customized with environment variables to control
          the installation directory and other options.
        </p>

        <h3>Docker</h3>
        <p>
          azd is also available as a Docker image for CI/CD pipelines and containerized environments.
        </p>
      </LearnMoreSection>
    </div>

    <!-- Navigation -->
    <TourNavigation 
      currentStep={step.number}
      totalSteps={tourSteps.length}
      previousStep={prevStep}
      nextStep={nextStep}
    />
  </article>
</TourLayout>

<style>
  .tour-step-page {
    max-width: 100%;
  }

  .tour-step-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border-default);
  }

  .tour-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-bottom: 1rem;
  }

  .tour-breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .tour-breadcrumb a:hover {
    color: var(--color-interactive-default);
    text-decoration: underline;
  }

  .tour-step-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .tour-step-icon {
    font-size: 1.5rem;
  }

  .tour-step-number {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-azure-600);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  [data-theme="dark"] .tour-step-number {
    color: var(--color-azure-400);
  }

  .tour-step-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
  }

  .tour-step-info {
    margin-bottom: 1.5rem;
  }

  .tour-step-time {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .tour-step-content {
    line-height: 1.7;
  }

  .tour-step-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 2rem 0 1rem;
  }

  .tour-step-content h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 1.5rem 0 0.75rem;
  }

  .tour-step-content p {
    color: var(--color-text-secondary);
    margin: 0 0 1rem;
  }

  .tour-step-content ul,
  .tour-step-content ol {
    color: var(--color-text-secondary);
    padding-left: 1.5rem;
    margin: 0 0 1rem;
  }

  .tour-step-content li {
    margin: 0.5rem 0;
  }

  .tour-step-intro {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  @media (max-width: 640px) {
    .tour-step-title {
      font-size: 1.5rem;
    }
  }
</style>
