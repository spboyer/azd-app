[tool.poetry]
name = "test-poetry-project"
version = "0.1.0"
description = "Test project for poetry"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
six = "^1.16.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
