# yaml-language-server: $schema=https://raw.githubusercontent.com/jongio/azd-app/main/schemas/v1.1/azure.yaml.json

# Test: azure.yaml with EMPTY reqs array (reqs: [])
# Expected: reqs --generate should REPLACE empty array with detected requirements

name: reqs-generate-empty-reqs

reqs: []

services:
  web:
    project: .
    host: staticwebapp
    language: js
