# yaml-language-server: $schema=https://raw.githubusercontent.com/jongio/azd-app/main/schemas/v1.1/azure.yaml.json

# Test: azure.yaml with NO reqs section
# Expected: reqs --generate should ADD the reqs section with detected requirements

name: reqs-generate-no-reqs

services:
  web:
    project: .
    host: staticwebapp
    language: js
