# Python API

Simple Flask API that serves JSON data.

## Endpoints

- `GET /` - Welcome message
- `GET /api/data` - Get sample data items
- `GET /api/health` - Health check

## Running

```bash
pip install -r requirements.txt
python app.py
```

The API will start on http://localhost:8000
