# Node.js Web App

Simple Express web application that calls the Python API.

## Features

- Serves HTML interface at http://localhost:3000
- Proxies requests to Python API
- Shows API health status
- Displays data fetched from API

## Running

```bash
npm install
npm run dev
```

The web app will start on http://localhost:3000
